import styled from "styled-components";

const SideScrollerstyled = styled.div`
   scrollbar-color: rgba(0, 0, 0, 0.2) hsla(0, 0%, 100%, 0.1);
   scroll-behavior:smooth;
   scrollbar-width: none;`



export default SideScrollerstyled;